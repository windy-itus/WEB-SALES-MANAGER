<div class="main">
    <div class="content container-fluid">
        <div class="heading">
            <h3>Quản lý đơn đặt hàng</h3>
            <br>
        </div>
        <div class="clear"></div>
        <div class=" content_top table-responsive col-md-12">
            <table class="table table-striped table-sm">
                <thead>
                    <tr>
                        <th>ID đơn hàng</th>
                        <th>Ngày đặt</th>
                        <th>ID Người đặt</th>
                        <th>Trạng thái</th>
                    </tr>
                </thead>
                <tbody>
                    {{#if Data}}
                    {{#each Data}}
                    <tr>
                        <td>{{this.id}}</td>
                        <td>{{this.date}}</td>
                        <td>{{this.ID_Usser}}</td>
                        <td>{{this.status}}</td>
                        <td><a href="/admin/detail-order-{{this.id}}"> <button class="Button" type="submit"> Chi tiết
                                </button></a></td>
                        <td><a href="/admin/delete-order-{{this.id}}"> <button class="Button" type="submit"> Xóa
                                </button></a></td>
                    </tr>
                    {{/each}}
                    {{/if}}
                </tbody>
            </table>
        </div>
    </div>
</div>